export type ComponentType = "button" | "card" | "modal" | "input" | "navbar" | "alert" | "dropdown" | "badge" | "avatar" | "footer";

export const COMPONENT_TYPES: ComponentType[] = [
  "button", "card", "modal", "input", "navbar", "alert", "dropdown", "badge", "avatar", "footer"
];

export const DEFAULT_TAILWIND_CLASSES = "p-4 bg-gray-100 rounded-lg";